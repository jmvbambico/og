# Nothing machine-local belongs here. Kept as a guard against accidental adds:
# tokens, the cookie secret, host_id and chat.db all live under ~/.omnigent.
*.bak
__pycache__/
*.pyc
.pytest_cache/
og-install.json
auth_tokens.json
.cookie-secret
